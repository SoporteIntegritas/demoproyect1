<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="view_mx_integritas_purchase_tree_inherit" model="ir.ui.view">
            <field name="name">mx.purchase.tree.inherit</field>
            <field name="model">purchase.order</field>
            <field name="inherit_id" ref="purchase.purchase_order_tree"/>
            <field name="arch" type="xml">
              <xpath expr="//field[@name='partner_id']" position="after"> 
                <field name="requisition_id"/>
            </xpath>       
            </field>
        </record>

        <record model="ir.actions.act_window" id="action_purchase_costo_unitario">
        <field name="name">Costo Unitario</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">purchase.order.line</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>

    </record>
        
        <menuitem
        id="menu_purchase_costo_unitario"
        sequence="10"
        parent="purchase.menu_procurement_management"
        action="action_purchase_costo_unitario"/>




        <record model="ir.ui.view" id="view_purchase_costo_unitario_tree">
        <field name="name">purchase.costo.unitario.tree</field>
        <field name="model">purchase.order.line</field>
        
        <field name="arch" type="xml">
            <tree string="Costo Unitario">
                <field name="order_id"/>
                <field name="partner_id"/>
                <field name="product_id"/>
                <field name="price_unit"/>
                <field name="product_qty"/>
                <field name="product_uom"/>
                <field name="price_subtotal"/>
                <field name="date_planned"/>
                <field name="requisition_id"/>
                
                
                
              
            </tree>
        </field>
        </record>

    </data>
</odoo>